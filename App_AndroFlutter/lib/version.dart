/// Versione mostrata nell'AppBar, utile per verificare a colpo d'occhio se
/// il browser sta servendo l'ultima build (es. dopo un aggiornamento bloccato
/// dalla cache del service worker di Flutter Web). Da incrementare ad ogni
/// nuovo rilascio, insieme al campo `version:` in pubspec.yaml.
const String appVersion = 'v1.0.1';
