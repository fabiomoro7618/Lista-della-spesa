services:
  - type: web
    name: spesa-ocr-backend
    env: python
    plan: free
    rootDir: App_Python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
